[phases.setup]
nixPkgs = ["nodejs_22"]
[phases.install]
cmds = ["npm install"]
[phases.build]
cmds = [
  "npx prisma generate",
  "npx prisma migrate deploy",
  "npx tsc --project tsconfig.json"
]

[start]
cmd = "node dist/server.js"